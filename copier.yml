# additional questions
---
use_devcontainer:
    type: bool
    default: false
    help: Do you want to support a DevContainer?

use_latex:
    type: bool
    default: false
    help: Do you want to add a LaTeX document?

use_dependabot:
    type: bool
    default: false
    help: Do you want to use Dependabot?

use_renovate:
    type: bool
    default: false
    help: Do you want to use Renovate?
---

# Require that the user has at least Copier v9.1.0 installed
# Earliest version with "multiselect" support.
_min_copier_version: "9.1.0"
_subdirectory: "template"
_answers_file: .copier-answers.yml
_overwrite: true
